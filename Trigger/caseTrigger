trigger caseTrigger on case (before insert,after insert,before update,after update,before delete,after delete,after undelete){
    if(trigger.IsBefore){
        if(trigger.IsInsert){

             caseTriggerHandler.setStatus(trigger.new);
            
        }
    }
     if(trigger.isAfter){
        if(trigger.IsInsert){
           
             caseTriggerHandler.updateAcc(trigger.new);
         
        
        }
    }

}
