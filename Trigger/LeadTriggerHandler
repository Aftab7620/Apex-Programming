public class leadTriggerHandler {
    
    public static void markRating(list<lead> newlist){
        for(Lead ld:newlist){
            if(ld.leadSource=='Web'){
                ld.rating='Hot';
            }
        }
    }
}
